name: Deploy Website

on:
  push:
    branches:
      - main  # یا master (نام برنچ اصلی خود را چک کنید)

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: /public_html/
        protocol: ftp          # 👈 تغییر مهم: استفاده از پروتکل ساده
        port: 21               # 👈 پورت استاندارد
        security: loose        # 👈 گیر ندادن به گواهی امنیتی
        timeout: 30000         # 👈 افزایش زمان انتظار (برای فایل‌های حجیم)
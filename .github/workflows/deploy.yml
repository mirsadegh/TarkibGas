name: Deploy Website

on:
  push:
    branches:
      - main  # یا master (نام برنچ اصلی خود را چک کنید)

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./ # پوشه لوکال (اگر فایل‌های سایت در روت است)
        server-dir: /public_html/ # مسیری که در هاست باید فایل‌ها ریخته شوند